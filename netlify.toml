[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[edge_functions]]
  path = "/api/agents/*"
  function = "next-runtime"

# Landing page: serve static HTML at root instead of Next.js redirect
[[redirects]]
  from = "/"
  to = "/landing.html"
  status = 200
  force = true
