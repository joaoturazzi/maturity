// TODO: Implement action plan card with completion percentage
'use client';

import type { ActionPlanWithTasks } from '@/types/actionPlan';

export function PlanCard({ plan: _plan }: { plan: ActionPlanWithTasks }) {
  return <div>{/* TODO: Plan card with title, priority, progress bar */}</div>;
}
